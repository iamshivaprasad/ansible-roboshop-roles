[defaults]
forks = 7 # default is 5, increase it to 10 or 20 for faster execution
inventory = ./inventory.ini
log_path = /var/log/roboshop/ansible.log
# vault_password_file = /etc/ansible/mysql_vault_pass.txt